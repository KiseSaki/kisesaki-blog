<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kisesaki.blog.content.interaction.mapper.CustomEventsMapper">

    <!-- 批量插入事件记录 -->
    <insert id="batchInsertEvents">
        INSERT INTO custom_events (
            user_id, event_type, event_data, page_url, session_id,
            ip_address, user_agent, device_type, browser, os, created_at
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.userId}, #{item.eventType}, #{item.eventData}, #{item.pageUrl},
                #{item.sessionId}, #{item.ipAddress}, #{item.userAgent}, #{item.deviceType},
                #{item.browser}, #{item.os}, #{item.createdAt}
            )
        </foreach>
    </insert>

</mapper>