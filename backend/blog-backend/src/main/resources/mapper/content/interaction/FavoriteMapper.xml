<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kisesaki.blog.content.interaction.mapper.FavoriteMapper">

    <!-- 查找用户对指定文章的收藏记录 -->
    <select id="findUserFavorite" resultType="com.kisesaki.blog.content.interaction.entity.Favorites">
        SELECT * FROM favorites 
        WHERE user_id = #{userId} AND post_id = #{postId}
        LIMIT 1
    </select>

    <!-- 批量查找用户对多篇文章的收藏记录 -->
    <select id="findUserFavoritesByPostIds" resultType="com.kisesaki.blog.content.interaction.entity.Favorites">
        SELECT * FROM favorites 
        WHERE user_id = #{userId} 
        AND post_id IN 
        <foreach collection="postIds" item="postId" open="(" separator="," close=")">
            #{postId}
        </foreach>
    </select>

    <!-- 统计文章的收藏数量 -->
    <select id="countFavoritesByPost" resultType="int">
        SELECT COUNT(*) FROM favorites WHERE post_id = #{postId}
    </select>

    <!-- 统计用户的收藏总数 -->
    <select id="countUserFavorites" resultType="int">
        SELECT COUNT(*) FROM favorites WHERE user_id = #{userId}
    </select>

    <!-- 分页查询文章的收藏用户列表 -->
    <select id="selectFavoritesByPost" resultType="com.kisesaki.blog.content.interaction.entity.Favorites">
        SELECT f.* FROM favorites f
        WHERE f.post_id = #{postId}
        ORDER BY f.created_at DESC
    </select>

    <!-- 分页查询用户的收藏列表 -->
    <select id="selectFavoritesByUser" resultType="com.kisesaki.blog.content.interaction.entity.Favorites">
        SELECT f.* FROM favorites f
        WHERE f.user_id = #{userId}
        ORDER BY f.created_at DESC
    </select>

    <!-- 删除用户对指定文章的收藏 -->
    <delete id="deleteFavorite">
        DELETE FROM favorites 
        WHERE user_id = #{userId} AND post_id = #{postId}
    </delete>

    <!-- 批量删除用户的收藏记录 -->
    <delete id="deleteFavoritesByPostIds">
        DELETE FROM favorites 
        WHERE user_id = #{userId} 
        AND post_id IN 
        <foreach collection="postIds" item="postId" open="(" separator="," close=")">
            #{postId}
        </foreach>
    </delete>

</mapper>