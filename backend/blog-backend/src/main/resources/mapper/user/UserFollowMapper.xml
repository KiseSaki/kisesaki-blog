<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kisesaki.blog.user.mapper.UserFollowMapper">

    <!-- 获取用户关注的人列表 -->
    <select id="findFollowingsByFollower" resultType="com.kisesaki.blog.user.entity.UserFollow">
        SELECT * FROM user_follows 
        WHERE follower_id = #{followerId} 
        AND status = #{status}
    </select>

    <!-- 获取关注某用户的人列表 -->
    <select id="findFollowersByFollowing" resultType="com.kisesaki.blog.user.entity.UserFollow">
        SELECT * FROM user_follows 
        WHERE following_id = #{followingId} 
        AND status = #{status}
    </select>

    <!-- 统计用户关注的人数 -->
    <select id="countFollowingsByFollower" resultType="long">
        SELECT COUNT(*) FROM user_follows 
        WHERE follower_id = #{followerId} 
        AND status = #{status}
    </select>

    <!-- 统计关注某用户的人数 -->
    <select id="countFollowersByFollowing" resultType="long">
        SELECT COUNT(*) FROM user_follows 
        WHERE following_id = #{followingId} 
        AND status = #{status}
    </select>

    <!-- 检查是否已关注 -->
    <select id="isFollowing" resultType="boolean">
        SELECT COUNT(*) > 0 FROM user_follows 
        WHERE follower_id = #{followerId} 
        AND following_id = #{followingId} 
        AND status = #{status}
    </select>

</mapper>